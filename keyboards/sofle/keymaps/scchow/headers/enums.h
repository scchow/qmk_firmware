#pragma once

enum sofle_layers {
    _DEFAULTS = 0,
    _BASE = 0,
    _NAV,
    _NAV2,
    _GAME,
    _GAME2,
    _NUMPAD,
    _SWITCH,
    _CONFIG
};

enum custom_keycodes {
    KC_BASE = SAFE_RANGE,
    KC_D_MUTE
};

enum {
    TD_LCTL_LALT,
};
